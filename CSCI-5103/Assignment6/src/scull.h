#ifndef _SCULL_H_
#define _SCULL_H_

//#include <linux/fs.h>	// for inode, file

// Prototypes for shared functions
// Used by producers & consumers in main and defined in buffer.c
//int     scullb_init(void);
//void    scullb_release(struct inode *inode, struct file *filp);
//ssize_t scullb_read(struct file *filp, char __user *buf, size_t count, loff_t *f_pos);
//ssize_t scullb_write(struct file *filp, const char __user *buf, size_t count, loff_t *f_pos);

#endif	// _SCULL_H_
